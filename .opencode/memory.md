# Mission Control UI — Session Memory

2026-06-26: Fixed Mission Probe agent hardcoded URLs (audit.simplyict.com.au → mc.simplyict.com.au) in 7 files: install.ps1, install.sh, config.toml.example, app.py, help.html, site/help.html. Discovered Cloudflare Access on mc.simplyict.com.au blocks /monitoring-api/* — 78 probes offline. Probes need CF Access bypass rule or service tokens to connect.

2026-06-29 (session 11): Designed MC Launchpad upgrade — search bar + tag filter pills + favorites/pinned + collapsible sections for the 32-app landing page. Spec written at docs/superpowers/specs/2026-06-29-mc-launchpad-design.md. Waiting for user approval before implementation.
2026-06-29 (session 11): Implemented MC Launchpad — search bar (Ctrl+K, /, Escape), tag filter pills (Dashboard, Vault, Tool, App, Kanban, Link, SOC, Alerts, Admin, Bridge), favorites/pinned (localStorage), collapsible sections (localStorage). Single file change to index.html (250→434 lines). Awaiting user verification on live page.

2026-07-03: Added error handling to device-audit.html — loadSites/loadResumeAudits/initPage now catch fetch failures, show user-facing error message on status line, and log to console. Fixes silent empty site dropdown.

2026-07-10: Production recovery on 208.87.135.193 (mc.simplyict.com.au). Fixed 4 down sub-apps causing 502 gateway errors on dashboard cards: Credential System (port 5001), Network ECCM (6173), OpenCode MC Dashboard (51999), Project Wizard (61738). Created 5 new systemd services (credential-system, network-infra, opencode-mc, project-wizard, device-audit-api) with Restart=always + enabled. Fixed duplicate `host=` kwarg bug in network-infra/backend/app/switch_ssh.py and gwn7811p_interrogate.py. Fixed log_message IndexError in opencode-mc/server.py. Created healthcheck-watcher systemd service — polls all 7 services every 30s, auto-restarts any down service + reloads nginx. Fixed nginx /wizard/ rewrite rule. All 9 services active+enabled across production server.

2026-09-17 (SOC P1.1): Agent auto-update shipped and verified. Server publishes one artifact via /api/agent/download/agent with X-Agent-Version/X-Agent-Sha256; the version+hash are parsed from agent_unified.py (soc_api.agent_meta(), mtime-cached) so a release is a one-line AGENT_VERSION bump. Registration ack now sends download_url (built from the host the agent reached), sha256 and a semver needs_update; the agent verifies sha+version, syntax-compiles, backs up to agent.py.bak, os.replace()s atomically, reports {"type":"update"} and re-execs — one attempt per process. Two long-standing bugs found while testing: (1) main() sat above six @handler definitions, so eventlog/forward_logs/self_update/fim_scan/packages/vuln_packages never registered when the agent ran as a script (server-pushed commands — including every queued FIM/vuln scan — were silently ignored); the __main__ guard now lives at the end of the file. (2) the on-connect update path referenced _aiohttp/_json, which only exist when aiohttp is MISSING, so every agent raised NameError and skipped the update — that was the real "chicken-and-egg". Outbox: delivered commands are marked 'sent' (no more 5s re-send spam) and unanswered ones are re-delivered on reconnect. Fleet note: agents below 1.1.1 cannot self-update (bug 1 + bug 2 live in their code) — they need one installer push; verified end-to-end locally with a 1.1.0-stamped agent that updated itself to 1.1.1 and reconnected.
